/**
 * Публичные адреса проекта — единственный источник правды.
 *
 * Бренд: «Вершина Души» (ru) / Apex Soul (en).
 */

/**
 * Основной публичный домен лендинга.
 * Используется для canonical, OG, JSON-LD и абсолютных ссылок.
 *
 * ВАЖНО: то же значение продублировано литералом в `astro.config.mjs → site`
 * (конфиг Astro — .mjs и не может импортировать .ts). При смене домена
 * менять оба места + `public/robots.txt`.
 */
export const SITE_URL = 'https://apexsoul.life';

/**
 * Адрес веб-приложения.
 *
 * Этап 1 (сейчас): остаётся рабочий https://soulbook.life — поддомен
 * app.apexsoul.life ещё не подключён и не протестирован.
 * Этап 2: заменить на 'https://app.apexsoul.life' — правка только здесь.
 *
 * Кнопки входа на лендинге сейчас ведут в Telegram-бота (см. BOT_URL),
 * а не на домен приложения напрямую.
 */
export const APP_URL = 'https://soulbook.life';

/**
 * Точка входа пользователя с лендинга — Telegram-бот.
 * Технический идентификатор бота при ребренде НЕ меняется.
 */
export const BOT_URL = 'https://t.me/Soulbook2_bot';
